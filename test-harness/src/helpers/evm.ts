export function toWei(amount: string, decimals = 18): string { return (Number(amount) * 10 ** decimals).toString(); } export function fromWei(amount: string, decimals = 18): string { return (Number(amount) / 10 ** decimals).toString(); } export function randomAddress(): string { const hex = Array.from({ length: 40 }, () => Math.floor(Math.random() * 16).toString(16)).join(""); return "0x" + hex; }
