export function assertBalance(actual: string, expected: string, tolerance = "0.01"): void { const diff = Math.abs(Number(actual) - Number(expected)); if (diff > Number(tolerance)) throw new Error(`Balance mismatch: ${actual} vs ${expected} (tolerance: ${tolerance})`); } export function assertTxSuccess(receipt: { status: string }): void { if (receipt.status !== "0x1" && receipt.status !== "confirmed") throw new Error(`Transaction failed: ${receipt.status}`); } export function assertApprox(actual: number, expected: number, tolerancePercent = 5): void { const diff = Math.abs(actual - expected) / expected * 100; if (diff > tolerancePercent) throw new Error(`Approx check failed: ${actual} vs ${expected} (${diff.toFixed(2)}% diff)`); }
