{
  "name": "@jellychain/chain-connector",
  "version": "1.0.0",
  "description": "Unified RPC connector for 20+ blockchain networks — balances, transactions, events, traces",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@jellychain/shared-types": "*",
    "@jellychain/sdk-core": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.12.0",
    "vitest": "^1.6.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jelly-chain/SDK.git",
    "directory": "chain-connector"
  }
}
